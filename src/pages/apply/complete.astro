---
import Layout from '../../layouts/Layout.astro';
import { GAS_URL } from '../../consts';

const base = import.meta.env.BASE_URL;
const link = (p: string) => `${base.replace(/\/$/, '')}/${p.replace(/^\//, '')}`;
---

<Layout title="お申し込み完了｜Branch" description="講座のお申し込み完了ページです。">
  <section class="section">
    <div class="container narrow">
      <div id="card" class="complete-card">
        <div class="spinner" id="spinner" aria-hidden="true"></div>
        <h1 id="c-title" class="c-title">お支払いを確認しています…</h1>
        <p id="c-desc" class="c-desc">しばらくお待ちください。</p>
        <div id="c-summary" class="c-summary" hidden></div>
        <div id="c-actions" class="c-actions" hidden>
          <a class="btn btn-primary" href={link('/courses')}>講座一覧へ</a>
          <a class="btn btn-ghost" href={link('/')}>トップへ戻る</a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script define:vars={{ GAS_URL }}>
  const spinner = document.getElementById('spinner');
  const titleEl = document.getElementById('c-title');
  const descEl = document.getElementById('c-desc');
  const summaryEl = document.getElementById('c-summary');
  const actionsEl = document.getElementById('c-actions');
  const card = document.getElementById('card');

  function done(state) {
    spinner.style.display = 'none';
    actionsEl.hidden = false;
    card.classList.add(state);
  }

  (async function confirm() {
    const sessionId = new URLSearchParams(window.location.search).get('session_id');
    if (!sessionId) {
      titleEl.textContent = 'ページを開けませんでした';
      descEl.textContent = '決済情報が見つかりません。お手数ですが申込ページからやり直してください。';
      done('error');
      return;
    }
    try {
      const res = await fetch(GAS_URL + '?action=course_confirm&session_id=' + encodeURIComponent(sessionId));
      const data = await res.json();
      if (data.status === 'confirmed') {
        titleEl.textContent = 'お申し込みが完了しました';
        descEl.textContent = 'お支払いを確認しました。受講のご案内は担当よりご連絡いたします。';
        const s = data.summary || {};
        const rows = [
          s.course ? ['講座', s.course] : null,
          s.name ? ['お名前', s.name] : null,
          s.amount ? ['お支払い', '¥' + Number(s.amount).toLocaleString('ja-JP')] : null,
          s.isMember ? ['受講履歴', '会員情報に自動で反映されました'] : null,
        ].filter(Boolean);
        if (rows.length) {
          summaryEl.innerHTML = rows
            .map((r) => `<div class="row"><span>${r[0]}</span><strong>${r[1]}</strong></div>`)
            .join('');
          summaryEl.hidden = false;
        }
        done('success');
      } else if (data.status === 'pending') {
        titleEl.textContent = 'お支払いが確認できませんでした';
        descEl.textContent = 'お支払いが完了している場合は、少し時間をおいて再読み込みしてください。';
        done('error');
      } else {
        titleEl.textContent = '確認に失敗しました';
        descEl.textContent = data.message || 'お手数ですが運営までお問い合わせください。';
        done('error');
      }
    } catch (e) {
      titleEl.textContent = '通信エラー';
      descEl.textContent = 'ネットワークをご確認のうえ、ページを再読み込みしてください。';
      done('error');
    }
  })();
</script>

<style>
  .narrow {
    max-width: 560px;
  }
  .complete-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius, 14px);
    padding: 3rem 2rem;
    text-align: center;
    box-shadow: var(--shadow, 0 10px 30px rgba(0, 0, 0, 0.06));
  }
  .complete-card.success {
    border-color: var(--color-primary);
  }
  .complete-card.error {
    border-color: #e0b4b4;
  }
  .c-title {
    font-size: clamp(1.4rem, 4vw, 1.9rem);
    margin-bottom: 0.8rem;
  }
  .complete-card.success .c-title {
    color: var(--color-primary-soft);
  }
  .c-desc {
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
  }
  .c-summary {
    display: grid;
    gap: 0.5rem;
    text-align: left;
    max-width: 360px;
    margin: 0 auto 1.8rem;
    background: var(--color-bg-soft);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1rem 1.2rem;
  }
  .c-summary .row {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    font-size: 0.92rem;
  }
  .c-summary .row span {
    color: var(--color-text-muted);
  }
  .c-actions {
    display: flex;
    gap: 0.8rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .spinner {
    width: 38px;
    height: 38px;
    margin: 0 auto 1.5rem;
    border: 3px solid var(--color-border);
    border-top-color: var(--color-primary);
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
  }
  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }
</style>
