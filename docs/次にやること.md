# 次にやること（TODO）

最終更新: 2026-07-24 / 作業を一旦停止。再開時はここから。

対象：Branch（占い師コミュニティ）と Fortune Lab☆！（鑑定サイト）。
各機能の詳細は同フォルダの個別docs参照。

---

## A. すぐやる運用作業（あなた側・実装は完了済み）

### A-1. Branch GAS の再デプロイ（未反映のコードがある）
`gas/` の最新を Apps Script（**Branch Webアプリ**）に反映：
- [ ] `OfflinePayment.gs` を**新規追加**（対面決済QR。まだ貼っていない）
- [ ] `Code.gs` を最新に差し替え（offline系ルート・course_confirm を追加済み）
- [ ] 新DBをインテグレーションに接続：**講座申込DB / 対面決済DB / 会員の声DB / インスタ投稿DB**
      （「サイト・講座」ページごと接続すると楽）
- [ ] 「新バージョン」でデプロイ

### A-2. Notion 入力
- [ ] 講座DB：販売する講座に `会員価格`/`非会員価格` を入力し `決済対象` にチェック
- [ ] 会員へ **会員番号（BR-001〜）** を案内（会員DBの「会員番号」欄）
- [ ] `src/consts.ts` の `INSTAGRAM_URL` にプロフィールURLを設定（フォローボタン用）
- [ ] 会員の声DB：受講生の感想を入力（`掲載許可`＋`公開` の両方チェックで表示）
- [ ] インスタ投稿DB：載せたい投稿URLを貼って `公開`
- [ ] 占い師DB「沙織」：整数時モデルで近似中（20:30開始/隔週は未対応）。必要なら相談

---

## B. 決済の有効化（Stripe APIキー待ち）

キーが届いたら登録するだけで各機能が有効化される。
- [ ] **ラボ**：Fortune Lab Webアプリに `STRIPE_SECRET_KEY`（鑑定予約の前払い）
- [ ] **ブランチ**：Branch Webアプリに `STRIPE_SECRET_KEY`（講座決済・対面決済QR）
- [ ] まずテストモード（`sk_test_...`）→ テストカード 4242… で確認 → 本番キーへ

### B-1. ラボ決済を有効化する前に対応したいこと
- [ ] **時間別/メニュー別の料金**（現状は占い師ごとの固定額で、鑑定時間を変えても金額が変わらない）
- [ ] （任意）**Stripe Webhook** 追加＝ブラウザを閉じても予約確定が走る（100%自動化）

---

## C. 未実装の機能バックログ（優先度めやす）

### 高
- [ ] **サイトのランキングページ** `/ranking`（順位・合計点・称号バッジ。スコアが貯まったら）
- [ ] **Zoom参加者ログの自動取込**（定例会・セミナーを完全無人化。Zoom Pro＋Server-to-Server OAuth が必要）
- [ ] **皆勤賞の月次自動化**：GAS `awardStreakBadges` を月1回のトリガーに設定

### 中
- [ ] **受講数の自動カウント**：会員DBの `受講講座` からロールアップで自動集計（手入力を削減）
- [ ] **本人のスコア照会**：公式LINEで自分の内訳を返す（内訳を安全に見せる）
- [ ] **LINE強化**：リッチメニュー／自動応答（チャットボット）／「占い師志望」タグ→ブランチへステップ配信
- [ ] **ブログ個別化**（保留中）：占い師ごとに別ブログDB化して他人の記事を編集できないように
      → 各占い師のブログDBを作成しDB IDを共有 → ビルドを複数DB合算に変更

### 低 / 任意
- [ ] 成長賞（前期比の伸び率で表彰）／区分別ランキング（運営・一般）
- [ ] スプレッドシート⇄スコアDB連携（`ScoreSheetSync.gs` は作成済み・未セットアップ。
      ※自動集計項目はスプシから入力しない運用の徹底が前提）
- [ ] LINE Pay 追加、Instagram 最新投稿の自動フィード（Graph API）

---

## D. 完了済み（参考）

相互リンク／1時間ごと自動デプロイ／Actions最新化／サイト名を「Fortune Lab☆！」に／
ラボ→ブランチ導線(クッションページ)／LINE連携有効化／予約カレンダー(占い師別・10分刻み・
所要時間・インターバル)／会員スコア(Notion一元管理・四半期繰越)／出席受付QR＋スコア自動集計／
DBの部屋分け(会員・スコア運用／サイト・講座／Fortune Lab運営DB)／講座決済の基盤／
会員の声ページ／対面決済QR／Instagram埋め込み／マニュアル(索引・目次・全機能の章)。
